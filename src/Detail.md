# src 层说明

这一层是仓库真正的手写源码入口。

## 当前职责

- 承载 `main` 主源集。
- 区分代码与资源。

## 实现方式

当前项目没有 `test/`、`android/` 或其他自定义源集，说明它是一个非常精简的单模块模组仓库。

## 层级关系

- 上游只受根目录脚本与文档约束。
- 下游被 `build/classes` 和 `build/resources` 分别消费。
